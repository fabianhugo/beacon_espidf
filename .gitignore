.pio
.pioenvs
.piolibdeps
.cache
.clang-format
.DS_Store
.idea
.vscode
compile_commands.json
__pycache__/

# Build output / logs
build.log
build_c3.log
build_s3.log
/build/
/managed_components/
/dependencies.lock
sdkconfig.old

# Web flasher firmware binaries — build outputs, regenerated by
# webflash/prepare-bins.sh locally and by the deploy-webflasher workflow in CI.
webflash/*.bin
